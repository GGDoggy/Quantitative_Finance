from .time_averaged_random_cancellation import empty_outputs


ALGORITHM_NAME = "event_balanced_placeholder"


def simulate_virtual_best_orders(
    init,
    updates,
    trades,
    start_time,
    time_step,
    base_tick,
):
    return empty_outputs()
